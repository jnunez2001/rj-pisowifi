// ZenCafe Database Manager
// PostgreSQL connection pooling and query execution

#ifndef ZENCAFE_DATABASE_MANAGER_H
#define ZENCAFE_DATABASE_MANAGER_H

// Placeholder for database manager implementation
// Will handle (for the running server, not one-time setup):
// - PostgreSQL connection pooling
// - Query preparation and execution
// - Transaction management
// - Connection health checks
// - Query logging
//
// Migration running is a separate concern, already implemented in migration_runner.h/.cpp
// (a standalone one-time tool run via `zencafe-server --migrate`, not part of the
// connection-pooled request-handling path this class will cover).

#endif
